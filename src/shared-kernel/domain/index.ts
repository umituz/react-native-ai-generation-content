/**
 * Shared domain layer
 * Exports base strategy pattern for use across domains
 */

export type { IGenerationStrategy } from './base-generation-strategy';
export { BaseGenerationStrategy } from './base-generation-strategy';
